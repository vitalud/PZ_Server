<UserControl x:Class="Server.Views.UserControls.QuikControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Server.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top"
                        Orientation="Horizontal"
                        Margin="5">
                <TextBlock Text="Статус Quik:" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding IsConnected}" VerticalAlignment="Center"
                           Style="{StaticResource BoolTextBlockStyle}"
                           Margin="5, 0"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Instruments}" 
                          Style="{StaticResource DataGridStyle}" 
                          DockPanel.Dock="Top" 
                          Margin="5" 
                          FontSize="9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Статус" Binding="{Binding IsActive}"/>
                    <DataGridTextColumn Header="Инструмент" Binding="{Binding Name.Id}"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Name.Type}"/>
                    <DataGridTextColumn Header="AllAsks" Binding="{Binding Orders.AllAsks}"/>
                    <DataGridTextColumn Header="BestAsks" Binding="{Binding Orders.BestAsks}"/>
                    <DataGridTextColumn Header="NumAsks" Binding="{Binding Orders.NumAsks}"/>
                    <DataGridTextColumn Header="AllBids" Binding="{Binding Orders.AllBids}"/>
                    <DataGridTextColumn Header="BestBids" Binding="{Binding Orders.BestBids}"/>
                    <DataGridTextColumn Header="NumBids" Binding="{Binding Orders.NumBids}"/>
                    <DataGridTextColumn Header="TradesBuy" Binding="{Binding Trades.Buy}"/>
                    <DataGridTextColumn Header="TradesSell" Binding="{Binding Trades.Sell}"/>
                    <DataGridTextColumn Header="Volume" Binding="{Binding Trades.Volume}"/>
                    <DataGridTextColumn Header="Обновлено" Binding="{Binding LastUpdate}"/>
                </DataGrid.Columns>
                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <Grid>
                            <DataGrid x:Name="klinesGrid" 
                                      Style="{StaticResource DataGridStyle}" 
                                      ItemsSource="{Binding Klines}" 
                                      FontSize="9">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Период" Binding="{Binding Interval}"/>
                                    <DataGridTextColumn Header="День" Binding="{Binding Day}"/>
                                    <DataGridTextColumn Header="Время" Binding="{Binding Time}"/>
                                    <DataGridTextColumn Header="Open" Binding="{Binding Open}"/>
                                    <DataGridTextColumn Header="High" Binding="{Binding High}"/>
                                    <DataGridTextColumn Header="Low" Binding="{Binding Low}"/>
                                    <DataGridTextColumn Header="Close" Binding="{Binding Close}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
