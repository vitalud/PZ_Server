<UserControl x:Class="Server.Views.UserControls.StrategiesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Server.Views.UserControls"
             xmlns:str="clr-namespace:ProjectZeroLib;assembly=ProjectZeroLib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Items}" 
                  Style="{StaticResource DataGridStyle}"
                  Margin="5"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding Code}" CanUserResize="False"
                                        IsReadOnly="True" Width="100"/>
                <DataGridTextColumn Header="Сигнал" Binding="{Binding Signal.Status}" CanUserResize="False"
                                        IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
